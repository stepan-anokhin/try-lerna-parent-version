name: Canary Release
on:
  pull_request:
    types: [ opened, synchronize, labeled ]

jobs:
  release:
    runs-on: ubuntu-latest
    if: github.ref != 'refs/heads/fix-1'

    steps:
      - name: Imitate Canary Release
        uses: actions/github-script@v6
        with:
          script: |
            console.log(context);